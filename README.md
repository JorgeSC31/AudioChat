# Audio Chat for Unix Systems


## Compilar
**Dependencias**
- PortAudio
```console
sudo apt-get install libasound-dev portaudio19-dev libportaudio2 libportaudiocpp0
```

Para compilar:
```console
make config=release
```

Los archivos binarios se encontraran en la carpeta ```bin/```

